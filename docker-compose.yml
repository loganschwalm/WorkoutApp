services:
  workout-tracker:
    build: .
    container_name: workout-tracker
    restart: unless-stopped
    ports:
      - "6769:6769"
    volumes:
      - workout_data:/app/data
    environment:
      PORT: 6769
      WORKOUT_DB: /app/data/workouts.db
      # Set to "0" once your accounts exist, so nobody else can create one.
      ALLOW_REGISTRATION: "1"

volumes:
  workout_data:
