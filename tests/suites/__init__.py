"""Test suites. Each module exposes INTERCEPT and run(t)."""

# Ordered cheapest first, so a broken build fails fast.
NAMES = ['api', 'accounts', 'account_state', 'workout_flow', 'program', 'timer_and_sync', 'sound_settings', 'gym_usability', 'pwa', 'security', 'pages']
